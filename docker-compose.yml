# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres:16.0-alpine3.18
    restart: always
    environment:
      POSTGRES_USER: "hexagons"
      POSTGRES_PASSWORD: "b7L5451RReG1"
    volumes:
      - hexagons:/var/lib/postgresql/data
    ports:
      - 5432:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 8000:8080

volumes:
  hexagons:

